version: '3.9'

services:
  nginx:
    image: nginx:1.27-alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - nginx_logs:/var/log/nginx
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask
    restart: unless-stopped

  flask:
    build: .
    container_name: flask
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5000
      - LOG_MAX=10
    expose:
      - "5000"
    restart: unless-stopped

  cron:
    build: ./cron
    container_name: cron
    environment:
      - FLASK_INGEST_URL=http://flask:5000/ingest
      - NGINX_ACCESS_LOG=/var/log/nginx/access.log
    volumes:
      - nginx_logs:/var/log/nginx
    depends_on:
      - nginx
      - flask
    restart: unless-stopped

volumes:
  nginx_logs: